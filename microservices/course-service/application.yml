server:
  port: 8082

spring:
  application:
    name: edusphere-course-service
  datasource:
    url: jdbc:mysql://localhost:3308/course_db?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    username: root
    password: root

service:
  owns:
    - courses
    - modules
    - lessons
    - enrollments
    - assignments
    - quizzes
    - certificates
