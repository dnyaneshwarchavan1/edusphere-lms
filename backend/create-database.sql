CREATE DATABASE IF NOT EXISTS edusphere_lms
  CHARACTER SET utf8mb4
  COLLATE utf8mb4_unicode_ci;

USE edusphere_lms;

-- Spring Boot/Hibernate creates and updates the application tables automatically.
-- Run this file in MySQL Workbench only if your MySQL user cannot create the
-- database through the JDBC createDatabaseIfNotExist option.
